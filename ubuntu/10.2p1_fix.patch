Description: Add handshake obfuscation
Author: zinglau <zinglau2015@gmail.com>
Index: openssh-10.2p1/Makefile.in
===================================================================
--- openssh-10.2p1.orig/Makefile.in	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/Makefile.in	2026-05-15 09:36:21.015464172 +0800
@@ -108,7 +108,7 @@
 	kexgexc.o kexgexs.o kexgssc.o \
 	kexsntrup761x25519.o kexmlkem768x25519.o sntrup761.o kexgen.o \
 	sftp-realpath.o platform-pledge.o platform-tracing.o platform-misc.o \
-	sshbuf-io.o misc-agent.o
+	sshbuf-io.o misc-agent.o obfuscate.o
 
 P11OBJS= ssh-pkcs11-client.o
 
Index: openssh-10.2p1/kex.c
===================================================================
--- openssh-10.2p1.orig/kex.c	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/kex.c	2026-05-15 09:36:21.015464172 +0800
@@ -58,6 +58,9 @@
 #include "myproposal.h"
 #include "xmalloc.h"
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#include "obfuscate.h"
+#endif
 #include "ssherr.h"
 #include "sshbuf.h"
 #include "digest.h"
@@ -342,6 +345,11 @@
 	if (!ssh->kex->ext_info_c && !ssh->kex->ext_info_s)
 		return 0;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if (sshpkt_get_obfuscation(ssh) == 1)
+		sshpkt_disable_obfuscation(ssh);
+#endif
+
 	/* Compose EXT_INFO packet. */
 	if ((m = sshbuf_new()) == NULL)
 		fatal_f("sshbuf_new failed");
@@ -365,6 +373,10 @@
 
  fail:
 	sshbuf_free(m);
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if (sshpkt_get_obfuscation(ssh) == 0)
+		sshpkt_enable_obfuscation(ssh);
+#endif
 	return r;
 }
 
@@ -562,6 +574,10 @@
 	kex->flags &= ~KEX_INITIAL;
 	sshbuf_reset(kex->peer);
 	kex->flags &= ~KEX_INIT_SENT;
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if (sshpkt_get_obfuscation(ssh) == 1)
+		sshpkt_disable_obfuscation(ssh);
+#endif
 	return 0;
 }
 
@@ -1263,14 +1279,46 @@
 		goto out;
 	}
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	/* Make copy and obfuscate the original */
+	if (sshpkt_get_obfuscation(ssh) == 1) {
+		if ((cp = sshbuf_dup_string(our_version)) == NULL) {
+			error("%s: sshbuf_dup_string failed for obfuscation", __func__);
+			r = SSH_ERR_ALLOC_FAIL;
+			goto out;
+		}
+		obfuscate_output(sshbuf_mutable_ptr(our_version), sshbuf_len(our_version));
+	}
+#endif
+
 	if (atomicio(vwrite, ssh_packet_get_connection_out(ssh),
 	    sshbuf_mutable_ptr(our_version),
 	    sshbuf_len(our_version)) != sshbuf_len(our_version)) {
 		oerrno = errno;
 		debug_f("write: %.100s", strerror(errno));
 		r = SSH_ERR_SYSTEM_ERROR;
+		if (sshpkt_get_obfuscation(ssh) == 1) {
+			free(cp);
+		}
 		goto out;
 	}
+
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	/* Restore the original */
+	if (sshpkt_get_obfuscation(ssh) == 1) {
+		if ((r = sshbuf_consume(our_version, sshbuf_len(our_version))) != 0) {
+			error("%s: sshbuf_consume failed for obfuscation", __func__);
+			free(cp);
+			goto out;
+		}
+		if ((r = sshbuf_put(our_version, cp, strlen(cp))) != 0) {
+			error("%s: sshbuf_put failed for obfuscation", __func__);
+			free(cp);
+			goto out;
+		}
+	}
+#endif
+
 	if ((r = sshbuf_consume_end(our_version, 2)) != 0) { /* trim \r\n */
 		oerrno = errno;
 		error_fr(r, "sshbuf_consume_end");
@@ -1327,6 +1375,10 @@
 				r = SSH_ERR_SYSTEM_ERROR;
 				goto out;
 			}
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+			if(sshpkt_get_obfuscation(ssh) == 1)
+				obfuscate_input(&c, 1);
+#endif
 			if (c == '\r') {
 				expect_nl = 1;
 				continue;
Index: openssh-10.2p1/monitor.c
===================================================================
--- openssh-10.2p1.orig/monitor.c	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/monitor.c	2026-05-15 09:36:21.015464172 +0800
@@ -88,6 +88,10 @@
 #include "sk-api.h"
 #include "srclimit.h"
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#include "obfuscate.h"
+#endif
+
 #ifdef GSSAPI
 static Gssctxt *gsscontext = NULL;
 #endif
@@ -121,6 +125,10 @@
 int mm_answer_term(struct ssh *, int, struct sshbuf *);
 int mm_answer_state(struct ssh *, int, struct sshbuf *);
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+int mm_answer_obfuscation(struct ssh *, int, struct sshbuf *);
+#endif
+
 #ifdef USE_PAM
 int mm_answer_pam_start(struct ssh *, int, struct sshbuf *);
 int mm_answer_pam_account(struct ssh *, int, struct sshbuf *);
@@ -214,6 +222,9 @@
     {MONITOR_REQ_GSSCHECKMIC, MON_ONCE, mm_answer_gss_checkmic},
     {MONITOR_REQ_GSSSIGN, MON_ONCE, mm_answer_gss_sign},
 #endif
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	{MONITOR_REQ_OBF_STATE, MON_ONCE, mm_answer_obfuscation},
+#endif
     {0, 0, NULL}
 };
 
@@ -236,6 +247,9 @@
     {MONITOR_REQ_AUDIT_EVENT, MON_PERMIT, mm_answer_audit_event},
     {MONITOR_REQ_AUDIT_COMMAND, MON_PERMIT, mm_answer_audit_command},
 #endif
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	{MONITOR_REQ_OBF_STATE, 0, mm_answer_obfuscation},
+#endif
     {0, 0, NULL}
 };
 
@@ -299,6 +313,10 @@
 	monitor_permit(mon_dispatch, MONITOR_REQ_GSSSETUP, 1);
 #endif
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	monitor_permit(mon_dispatch, MONITOR_REQ_OBF_STATE, 1);
+#endif
+
 	/* The first few requests do not require asynchronous access */
 	while (!authenticated) {
 		partial = 0;
@@ -717,6 +735,17 @@
 	mm_request_send(sock, MONITOR_ANS_MODULI, m);
 	return (0);
 }
+
+#endif
+
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+int
+mm_answer_obfuscation(struct ssh *ssh, int sock, struct sshbuf *m) {
+	debug("sending current obfuscation key state");
+	obfuscate_send_keys(m);
+	mm_request_send(sock, MONITOR_ANS_OBF_STATE, m);
+	return (0);
+}
 #endif
 
 int
@@ -1945,7 +1974,6 @@
 	debug3_f("GOT new keys");
 }
 
-
 /* XXX */
 
 #define FD_CLOSEONEXEC(x) do { \
Index: openssh-10.2p1/monitor.h
===================================================================
--- openssh-10.2p1.orig/monitor.h	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/monitor.h	2026-05-15 09:36:21.019464295 +0800
@@ -56,6 +56,10 @@
 	MONITOR_REQ_TERM = 50,
 	MONITOR_REQ_STATE = 51, MONITOR_ANS_STATE = 52,
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	MONITOR_REQ_OBF_STATE = 91, MONITOR_ANS_OBF_STATE = 92,
+#endif
+
 	MONITOR_REQ_PAM_START = 100,
 	MONITOR_REQ_PAM_ACCOUNT = 102, MONITOR_ANS_PAM_ACCOUNT = 103,
 	MONITOR_REQ_PAM_INIT_CTX = 104, MONITOR_ANS_PAM_INIT_CTX = 105,
Index: openssh-10.2p1/monitor_wrap.c
===================================================================
--- openssh-10.2p1.orig/monitor_wrap.c	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/monitor_wrap.c	2026-05-15 09:36:21.019464295 +0800
@@ -949,6 +949,17 @@
 	debug3_f("done");
 }
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+void mm_get_obfuscation(struct sshbuf *m) {
+	debug3("sending MONITOR_REQ_OBF_STATE");
+	mm_request_send(pmonitor->m_recvfd, MONITOR_REQ_OBF_STATE, m);
+
+	debug3("waiting for MONITOR_ANS_OBF_STATE");
+	mm_request_receive_expect(pmonitor->m_recvfd,
+	    MONITOR_ANS_OBF_STATE, m);
+}
+#endif
+
 static void
 mm_chall_setup(char **name, char **infotxt, u_int *numprompts,
     char ***prompts, u_int **echo_on)
Index: openssh-10.2p1/monitor_wrap.h
===================================================================
--- openssh-10.2p1.orig/monitor_wrap.h	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/monitor_wrap.h	2026-05-15 09:36:21.019464295 +0800
@@ -103,6 +103,11 @@
     struct sshbuf **, uint64_t *, struct sshbuf **, struct sshbuf **,
     u_char **, struct sshbuf **, struct sshbuf **);
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+/* handshake obfuscation */
+void mm_get_obfuscation(struct sshbuf *);
+#endif
+
 /* bsdauth */
 int mm_bsdauth_query(void *, char **, char **, u_int *, char ***, u_int **);
 int mm_bsdauth_respond(void *, u_int, char **);
Index: openssh-10.2p1/obfuscate.c
===================================================================
--- /dev/null	1970-01-01 00:00:00.000000000 +0000
+++ openssh-10.2p1/obfuscate.c	2026-05-15 09:36:21.019464295 +0800
@@ -0,0 +1,247 @@
+#include "includes.h"
+#include <openssl/evp.h>
+#include <openssl/rc4.h>
+#include "openbsd-compat/openssl-compat.h"
+#include <unistd.h>
+#include <string.h>
+#include "atomicio.h"
+#include "canohost.h"
+#include "xmalloc.h"
+#include "log.h"
+#include "sshbuf.h"
+#include "packet.h"
+#include "obfuscate.h"
+
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+typedef struct {
+    RC4_KEY input;
+    RC4_KEY output;
+} RC4_KEYS;
+
+static const char *obfuscate_keyword = NULL;
+
+#define OBFUSCATE_KEY_LENGTH 	16
+#define OBFUSCATE_SEED_LENGTH	16
+#define OBFUSCATE_HASH_ITERATIONS 6000
+#define OBFUSCATE_MAX_PADDING	8192
+#define OBFUSCATE_MAGIC_VALUE	0x0BF5CA7E
+
+struct seed_msg {
+	u_char seed_buffer[OBFUSCATE_SEED_LENGTH];
+	u_int32_t magic;
+	u_int32_t padding_length;
+	u_char padding[];
+};
+
+static RC4_KEYS *rc4_keys;
+
+static void generate_key_pair(const u_char *, u_char *, u_char *);
+static void generate_key(const u_char *, const u_char *, u_int, u_char *);
+static void set_keys(const u_char *, const u_char *);
+static void initialize(const u_char *, int);
+static void read_forever(int);
+
+void
+obfuscate_recv_keys(struct sshbuf *m)
+{
+	if (!rc4_keys) {
+		rc4_keys = xmalloc(sizeof(RC4_KEYS));
+	}
+	sshbuf_get(m, (void *)rc4_keys, sizeof(RC4_KEYS));
+}
+
+void
+obfuscate_send_keys(struct sshbuf *m)
+{
+	sshbuf_put(m, (void *)rc4_keys, sizeof(RC4_KEYS));
+}
+
+/*
+ * Server calls this
+ */
+void
+obfuscate_receive_seed(struct ssh *ssh, int sock_in)
+{
+	struct seed_msg seed;
+
+	u_char padding_drain[OBFUSCATE_MAX_PADDING];
+	u_int len;
+	u_int32_t padding_length;
+
+	len = atomicio(read, sock_in, &seed, sizeof(struct seed_msg));
+
+	debug2("obfuscate_receive_seed: read %d byte seed message from client", len);
+	if(len != sizeof(struct seed_msg))
+		fatal("obfuscate_receive_seed: read failed");
+
+	initialize(seed.seed_buffer, 1);
+	obfuscate_input((u_char *)&seed.magic, 8);
+
+	if(OBFUSCATE_MAGIC_VALUE != ntohl(seed.magic)) {
+		logit("Magic value check failed (%u) on obfuscated handshake "
+ 			"from %.200s port %d", ntohl(seed.magic),
+			ssh_remote_ipaddr(ssh), ssh_remote_port(ssh));
+		read_forever(sock_in);
+	}
+	padding_length = ntohl(seed.padding_length);
+	if(padding_length > OBFUSCATE_MAX_PADDING) {
+		logit("Illegal padding length %d for obfuscated handshake "
+ 			"from %.200s port %d", ntohl(seed.padding_length),
+			ssh_remote_ipaddr(ssh), ssh_remote_port(ssh));
+		read_forever(sock_in);
+	}
+	len = atomicio(read, sock_in, padding_drain, padding_length);
+	if(len != padding_length)
+		fatal("obfuscate_receive_seed: read failed");
+	debug2("obfuscate_receive_seed: read %d bytes of padding from client.", len);
+	obfuscate_input(padding_drain, padding_length);
+}
+
+/*
+ * Client calls this
+ */
+void
+obfuscate_send_seed(int sock_out)
+{
+	struct seed_msg *seed;
+	int i;
+	u_int32_t rnd = 0;
+	u_int message_length;
+	u_int padding_length;
+
+	padding_length = arc4random() % OBFUSCATE_MAX_PADDING;
+	message_length = padding_length + sizeof(struct seed_msg);
+	seed = xmalloc(message_length);
+
+	for(i = 0; i < OBFUSCATE_SEED_LENGTH; i++) {
+		if(i % 4 == 0)
+			rnd = arc4random();
+		seed->seed_buffer[i] = rnd & 0xff;
+		rnd >>= 8;
+	}
+	seed->magic = htonl(OBFUSCATE_MAGIC_VALUE);
+	seed->padding_length = htonl(padding_length);
+	for(i = 0; i < (int)padding_length; i++) {
+		if(i % 4 == 0)
+			rnd = arc4random();
+		seed->padding[i] = rnd & 0xff;
+	}
+	initialize(seed->seed_buffer, 0);
+	obfuscate_output(((u_char *)seed) + OBFUSCATE_SEED_LENGTH,
+		message_length - OBFUSCATE_SEED_LENGTH);
+	debug2("obfuscate_send_seed: Sending seed message with %d bytes of padding", padding_length);
+	atomicio(vwrite, sock_out, seed, message_length);
+	free(seed);
+
+}
+
+void
+obfuscate_set_keyword(const char *keyword)
+{
+	debug2("obfuscate_set_keyword: Setting obfuscation keyword to '%s'", keyword);
+	obfuscate_keyword = keyword;
+}
+
+void
+obfuscate_input(u_char *buffer, u_int buffer_len)
+{
+	if (rc4_keys)
+		RC4(&rc4_keys->input, buffer_len, buffer, buffer);
+	else fatal("no obfuscation key");
+}
+
+void
+obfuscate_output(u_char *buffer, u_int buffer_len)
+{
+	if (rc4_keys)
+		RC4(&rc4_keys->output, buffer_len, buffer, buffer);
+	else fatal_f("no obfuscation key");
+}
+
+static void
+initialize(const u_char *seed, int server)
+{
+    u_char client_to_server_key[OBFUSCATE_KEY_LENGTH];
+	u_char server_to_client_key[OBFUSCATE_KEY_LENGTH];
+	generate_key_pair(seed, client_to_server_key, server_to_client_key);
+
+	rc4_keys = xmalloc(sizeof(RC4_KEYS));
+	if(server) {
+		set_keys(client_to_server_key, server_to_client_key);
+	} else {
+		set_keys(server_to_client_key, client_to_server_key);
+	}
+}
+
+static void
+generate_key_pair(const u_char *seed, u_char *client_to_server_key, u_char *server_to_client_key)
+{
+	generate_key(seed, "client_to_server", strlen("client_to_server"), client_to_server_key);
+	generate_key(seed, "server_to_client", strlen("server_to_client"), server_to_client_key);
+}
+
+static void
+generate_key(const u_char *seed, const u_char *iv, u_int iv_len, u_char *key_data)
+{
+	EVP_MD_CTX *ctx;
+	u_char md_output[EVP_MAX_MD_SIZE];
+	int md_len;
+	int i;
+	u_char *buffer;
+	u_char *p;
+	u_int buffer_length;
+
+	if ((ctx = EVP_MD_CTX_new()) == NULL)
+		fatal("Cannot create new digest context");
+
+	buffer_length = OBFUSCATE_SEED_LENGTH + iv_len;
+	if(obfuscate_keyword)
+		buffer_length += strlen(obfuscate_keyword);
+
+	p = buffer = xmalloc(buffer_length);
+
+	memcpy(p, seed, OBFUSCATE_SEED_LENGTH);
+	p += OBFUSCATE_SEED_LENGTH;
+
+	if(obfuscate_keyword) {
+		memcpy(p, obfuscate_keyword, strlen(obfuscate_keyword));
+		p += strlen(obfuscate_keyword);
+	}
+	memcpy(p, iv, iv_len);
+
+	EVP_DigestInit(ctx, EVP_sha1());
+	EVP_DigestUpdate(ctx, buffer, OBFUSCATE_SEED_LENGTH + iv_len);
+	EVP_DigestFinal(ctx, md_output, &md_len);
+
+	free(buffer);
+
+	for(i = 0; i < OBFUSCATE_HASH_ITERATIONS; i++) {
+		EVP_DigestInit(ctx, EVP_sha1());
+		EVP_DigestUpdate(ctx, md_output, md_len);
+		EVP_DigestFinal(ctx, md_output, &md_len);
+	}
+
+	if(md_len < OBFUSCATE_KEY_LENGTH)
+		fatal("Cannot derive obfuscation keys from hash length of %d", md_len);
+
+	memcpy(key_data, md_output, OBFUSCATE_KEY_LENGTH);
+}
+
+static void
+set_keys(const u_char *input_key, const u_char *output_key)
+{
+	RC4_set_key(&rc4_keys->input, OBFUSCATE_KEY_LENGTH, input_key);
+	RC4_set_key(&rc4_keys->output, OBFUSCATE_KEY_LENGTH, output_key);
+}
+
+static void
+read_forever(int sock_in)
+{
+	u_char discard_buffer[1024];
+
+	while(atomicio(read, sock_in, discard_buffer, sizeof(discard_buffer)) > 0)
+		;
+	cleanup_exit(255);
+}
+
+#endif
\ No newline at end of file
Index: openssh-10.2p1/obfuscate.h
===================================================================
--- /dev/null	1970-01-01 00:00:00.000000000 +0000
+++ openssh-10.2p1/obfuscate.h	2026-05-15 09:36:21.023464419 +0800
@@ -0,0 +1,14 @@
+#ifndef _OBFUSCATE_H
+#define _OBFUSCATE_H
+
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+void obfuscate_receive_seed(struct ssh *, int);
+void obfuscate_send_seed(int);
+void obfuscate_set_keyword(const char *);
+void obfuscate_input(u_char *, u_int);
+void obfuscate_output(u_char *, u_int);
+void obfuscate_recv_keys(struct sshbuf *);
+void obfuscate_send_keys(struct sshbuf *);
+#endif
+
+#endif
Index: openssh-10.2p1/packet.c
===================================================================
--- openssh-10.2p1.orig/packet.c	2025-10-10 10:38:31.000000000 +0800
+++ openssh-10.2p1/packet.c	2026-05-15 09:36:21.023464419 +0800
@@ -90,6 +90,7 @@
 #include "channels.h"
 #include "ssh.h"
 #include "packet.h"
+#include "obfuscate.h"
 #include "ssherr.h"
 #include "sshbuf.h"
 
@@ -170,6 +171,10 @@
 	/* Set to true if we are the server side. */
 	int server_side;
 
+	/* Set to 1 (0) if subsequent packets must (not) be obfuscated. */
+	/* Will be initialized to -1 and should only be toggled between 0/1 */
+	int obfuscation;
+
 	/* Set to true if we are authenticated. */
 	int after_authentication;
 
@@ -256,6 +261,7 @@
 	state->qos_interactive = state->qos_other = -1;
 	state->p_send.packets = state->p_read.packets = 0;
 	state->initialized = 1;
+	state->obfuscation = -1;
 	/*
 	 * ssh_packet_send2() needs to queue packets until
 	 * we've done the initial key exchange.
@@ -1313,6 +1319,10 @@
 		if ((r = sshbuf_put(state->output, macbuf, mac->mac_len)) != 0)
 			goto out;
 	}
+
+	if(state->obfuscation == 1)
+		obfuscate_output(cp, sshbuf_len(state->outgoing_packet));
+
 #ifdef PACKET_DEBUG
 	fprintf(stderr, "encrypted: ");
 	sshbuf_dump(state->output, stderr);
@@ -1633,6 +1643,8 @@
 		if ((r = sshbuf_reserve(state->incoming_packet, block_size,
 		    &cp)) != 0)
 			goto out;
+		if(state->obfuscation == 1)
+			obfuscate_input(sshbuf_mutable_ptr(state->input), block_size);
 		if ((r = cipher_crypt(state->receive_context,
 		    state->p_send.seqnr, cp, sshbuf_ptr(state->input),
 		    block_size, 0, 0)) != 0)
@@ -1698,6 +1710,8 @@
 			goto out;
 		}
 	}
+	if(state->obfuscation == 1)
+		obfuscate_input(sshbuf_mutable_ptr(state->input), need);
 	if ((r = sshbuf_reserve(state->incoming_packet, aadlen + need,
 	    &cp)) != 0)
 		goto out;
@@ -2902,3 +2916,23 @@
 	ssh->state->extra_pad = pad;
 	return 0;
 }
+
+void
+sshpkt_enable_obfuscation(struct ssh *ssh)
+{
+	ssh->state->obfuscation = 1;
+	debug("Obfuscation enabled");
+}
+
+void
+sshpkt_disable_obfuscation(struct ssh *ssh)
+{
+	ssh->state->obfuscation = 0;
+	debug("Obfuscation disabled");
+}
+
+int
+sshpkt_get_obfuscation(struct ssh *ssh)
+{
+	return ssh->state->obfuscation;
+}
Index: openssh-10.2p1/packet.h
===================================================================
--- openssh-10.2p1.orig/packet.h	2025-10-10 10:38:31.000000000 +0800
+++ openssh-10.2p1/packet.h	2026-05-15 09:36:21.023464419 +0800
@@ -183,6 +183,9 @@
 	    __attribute__((format(printf, 3, 4)))
 	    __attribute__((noreturn));
 int	sshpkt_msg_ignore(struct ssh *, u_int);
+void	sshpkt_enable_obfuscation(struct ssh *);
+void	sshpkt_disable_obfuscation(struct ssh *);
+int sshpkt_get_obfuscation(struct ssh *);
 
 int	sshpkt_put(struct ssh *ssh, const void *v, size_t len);
 int	sshpkt_putb(struct ssh *ssh, const struct sshbuf *b);
Index: openssh-10.2p1/readconf.c
===================================================================
--- openssh-10.2p1.orig/readconf.c	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/readconf.c	2026-05-15 09:36:21.023464419 +0800
@@ -143,6 +143,9 @@
 	oHost, oMatch, oInclude, oTag,
 	oForwardAgent, oForwardX11, oForwardX11Trusted, oForwardX11Timeout,
 	oGatewayPorts, oExitOnForwardFailure,
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	oObfuscateHandshake, oObfuscateKeyword,
+#endif
 	oPasswordAuthentication,
 	oXAuthLocation,
 	oIdentityFile, oHostname, oPort, oRemoteForward, oLocalForward,
@@ -347,6 +350,10 @@
 	{ "warnweakcrypto", oWarnWeakCrypto },
 	{ "protocolkeepalives", oProtocolKeepAlives },
 	{ "setuptimeout", oSetupTimeOut },
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	{ "obfuscatehandshake", oObfuscateHandshake },
+	{ "obfuscatekeyword", oObfuscateKeyword },
+#endif
 
 	{ NULL, oBadOption }
 };
@@ -2598,6 +2605,18 @@
 		}
 		break;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	case oObfuscateHandshake:
+		intptr = &options->obfuscate_handshake;
+		goto parse_flag;
+
+	case oObfuscateKeyword:
+		if (*activep)
+			options->obfuscate_handshake = 1;
+		charptr = &options->obfuscate_keyword;
+		goto parse_string;
+#endif
+
 	case oDeprecated:
 		debug("%s line %d: Deprecated option \"%s\"",
 		    filename, linenum, keyword);
@@ -2834,6 +2853,10 @@
 	options->add_keys_to_agent_lifespan = -1;
 	options->identity_agent = NULL;
 	options->visual_host_key = -1;
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	options->obfuscate_handshake = 0;
+	options->obfuscate_keyword = NULL;
+#endif
 	options->ip_qos_interactive = -1;
 	options->ip_qos_bulk = -1;
 	options->request_tty = -1;
Index: openssh-10.2p1/readconf.h
===================================================================
--- openssh-10.2p1.orig/readconf.h	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/readconf.h	2026-05-15 09:36:21.023464419 +0800
@@ -153,6 +153,11 @@
 	char	*remote_command;
 	int	visual_host_key;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	int obfuscate_handshake;
+	char *obfuscate_keyword;
+#endif
+
 	int	request_tty;
 	int	session_type;
 	int	stdin_null;
Index: openssh-10.2p1/scp.c
===================================================================
--- openssh-10.2p1.orig/scp.c	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/scp.c	2026-05-15 09:36:21.023464419 +0800
@@ -501,7 +501,7 @@
 
 	fflag = Tflag = tflag = 0;
 	while ((ch = getopt(argc, argv,
-	    "12346ABCTdfOpqRrstvD:F:J:M:P:S:c:i:l:o:X:")) != -1) {
+	    "12346ABCTdfOpqRrstvD:F:J:M:P:S:c:i:l:o:X:zZ:")) != -1) {
 		switch (ch) {
 		/* User-visible flags. */
 		case '1':
@@ -514,6 +514,7 @@
 		case '4':
 		case '6':
 		case 'C':
+		case 'z':
 			addargs(&args, "-%c", ch);
 			addargs(&remote_remote_args, "-%c", ch);
 			break;
@@ -531,6 +532,7 @@
 		case 'i':
 		case 'F':
 		case 'J':
+		case 'Z':
 			addargs(&remote_remote_args, "-%c", ch);
 			addargs(&remote_remote_args, "%s", optarg);
 			addargs(&args, "-%c", ch);
Index: openssh-10.2p1/servconf.c
===================================================================
--- openssh-10.2p1.orig/servconf.c	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/servconf.c	2026-05-15 09:36:21.023464419 +0800
@@ -220,6 +220,10 @@
 	options->sshd_auth_path = NULL;
 	options->refuse_connection = -1;
 	options->debian_banner = -1;
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	options->obfuscate_keyword = NULL;
+	options->num_obfuscated_ports = 0;
+#endif
 }
 
 /* Returns 1 if a string option is unset or set to "none" or 0 otherwise. */
@@ -319,7 +323,11 @@
 		    _PATH_HOST_ED25519_KEY_FILE, 0);
 	}
 	/* No certificates by default */
-	if (options->num_ports == 0)
+	if (options->num_ports == 0
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+		&& options->num_obfuscated_ports == 0
+#endif
+	)
 		options->ports[options->num_ports++] = SSH_DEFAULT_PORT;
 	if (options->address_family == -1)
 		options->address_family = AF_UNSPEC;
@@ -559,6 +567,9 @@
 	sUsePAM, sPAMServiceName,
 	/* Standard Options */
 	sPort, sHostKeyFile, sLoginGraceTime,
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	sObfuscatedPort, sObfuscateKeyword,
+#endif
 	sPermitRootLogin, sLogFacility, sLogLevel, sLogVerbose,
 	sKerberosAuthentication, sKerberosOrLocalPasswd, sKerberosTicketCleanup,
 	sKerberosGetAFSToken, sPasswordAuthentication,
@@ -619,6 +630,10 @@
 	{ "pamauthenticationviakbdint", sDeprecated, SSHCFG_GLOBAL },
 	/* Standard Options */
 	{ "port", sPort, SSHCFG_GLOBAL },
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	{ "obfuscatedport", sObfuscatedPort, SSHCFG_GLOBAL },
+	{ "obfuscatekeyword", sObfuscateKeyword, SSHCFG_GLOBAL },
+#endif
 	{ "hostkey", sHostKeyFile, SSHCFG_GLOBAL },
 	{ "hostdsakey", sHostKeyFile, SSHCFG_GLOBAL },		/* alias */
 	{ "hostkeyagent", sHostKeyAgent, SSHCFG_GLOBAL },
@@ -853,6 +868,12 @@
 			add_one_listen_addr(options, addr, rdomain,
 			    options->ports[i]);
 		}
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+		for (i = 0; i < options->num_obfuscated_ports; i++) {
+			add_one_listen_addr(options, addr, rdomain,
+				options->obfuscated_ports[i]);
+		}
+#endif
 	}
 }
 
@@ -967,7 +988,11 @@
 	u_int i;
 	struct queued_listenaddr *qla;
 
-	if (options->num_ports == 0)
+	if (options->num_ports == 0
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+		&& options->num_obfuscated_ports == 0
+#endif
+	)
 		options->ports[options->num_ports++] = SSH_DEFAULT_PORT;
 	if (options->address_family == -1)
 		options->address_family = AF_UNSPEC;
@@ -1447,6 +1472,32 @@
 			    filename, linenum);
 		break;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	case sObfuscatedPort:
+		if(options->ports_from_cmdline)
+			return 0;
+		if(options->listen_addrs != NULL)
+			fatal("%s line %d: ports must be specified before ListenAddress.", filename, linenum);
+		if(options->num_obfuscated_ports >= MAX_PORTS)
+			fatal("%s line %d: too many ports.", filename, linenum);
+		arg = argv_next(&ac, &av);
+		if(!arg || *arg == '\0')
+			fatal("%s line %d: missing port number.", filename, linenum);
+		options->obfuscated_ports[options->num_obfuscated_ports++] = a2port(arg);
+		if(options->obfuscated_ports[options->num_obfuscated_ports - 1] <= 0)
+			fatal("%s line %d: badly formatted port number.", filename, linenum);
+		break;
+	case sObfuscateKeyword:
+		charptr = &options->obfuscate_keyword;
+		arg = argv_next(&ac, &av);
+		if(!arg || *arg == '\0')
+			fatal("%s line %d: missing keyword argument.",
+					filename, linenum);
+		if(*activep && *charptr == NULL)
+			*charptr = xstrdup(arg);
+		break;
+#endif
+
 	case sLoginGraceTime:
 		intptr = &options->login_grace_time;
  parse_time:
@@ -3267,6 +3318,10 @@
 	/* these are usually at the top of the config */
 	for (i = 0; i < o->num_ports; i++)
 		printf("port %d\n", o->ports[i]);
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	for (i = 0; i < o->num_obfuscated_ports; i++)
+		printf("obfuscated port %d\n", o->obfuscated_ports[i]);
+#endif
 	dump_cfg_fmtint(sAddressFamily, o->address_family);
 
 	for (i = 0; i < o->num_listen_addrs; i++) {
@@ -3373,6 +3428,9 @@
 	dump_cfg_string(sSshdSessionPath, o->sshd_session_path);
 	dump_cfg_string(sSshdAuthPath, o->sshd_auth_path);
 	dump_cfg_string(sPerSourcePenaltyExemptList, o->per_source_penalty_exempt);
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	dump_cfg_string(sObfuscateKeyword, o->obfuscate_keyword);
+#endif
 
 	/* string arguments requiring a lookup */
 	dump_cfg_string(sLogLevel, log_level_name(o->log_level));
Index: openssh-10.2p1/servconf.h
===================================================================
--- openssh-10.2p1.orig/servconf.h	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/servconf.h	2026-05-15 09:36:21.027464544 +0800
@@ -224,6 +224,13 @@
 	u_int   num_permitted_listens;
 
 	char   *chroot_directory;
+
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	int obfuscated_ports[MAX_PORTS];
+	u_int num_obfuscated_ports;
+	char *obfuscate_keyword;
+#endif
+
 	char   *revoked_keys_file;
 	char   *trusted_user_ca_keys;
 	char   *authorized_keys_command;
Index: openssh-10.2p1/sftp.c
===================================================================
--- openssh-10.2p1.orig/sftp.c	2025-10-10 10:38:31.000000000 +0800
+++ openssh-10.2p1/sftp.c	2026-05-15 09:36:21.027464544 +0800
@@ -2463,13 +2463,14 @@
 	infile = stdin;
 
 	while ((ch = getopt(argc, argv,
-	    "1246AafhNpqrvCc:D:i:l:o:s:S:b:B:F:J:P:R:X:")) != -1) {
+	    "1246AafhNpqrvCc:D:i:l:o:s:S:b:B:F:J:P:R:X:zZ:")) != -1) {
 		switch (ch) {
 		/* Passed through to ssh(1) */
 		case 'A':
 		case '4':
 		case '6':
 		case 'C':
+		case 'z':
 			addargs(&args, "-%c", ch);
 			break;
 		/* Passed through to ssh(1) with argument */
@@ -2478,6 +2479,7 @@
 		case 'c':
 		case 'i':
 		case 'o':
+		case 'Z':
 			addargs(&args, "-%c", ch);
 			addargs(&args, "%s", optarg);
 			break;
Index: openssh-10.2p1/ssh.c
===================================================================
--- openssh-10.2p1.orig/ssh.c	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/ssh.c	2026-05-15 09:36:21.027464544 +0800
@@ -177,12 +177,13 @@
 usage(void)
 {
 	fprintf(stderr,
-"usage: ssh [-46AaCfGgKkMNnqsTtVvXxYy] [-B bind_interface] [-b bind_address]\n"
+"usage: ssh [-46AaCfGgKkMNnqsTtVvXxYyz] [-B bind_interface] [-b bind_address]\n"
 "           [-c cipher_spec] [-D [bind_address:]port] [-E log_file]\n"
 "           [-e escape_char] [-F configfile] [-I pkcs11] [-i identity_file]\n"
 "           [-J destination] [-L address] [-l login_name] [-m mac_spec]\n"
 "           [-O ctl_cmd] [-o option] [-P tag] [-p port] [-R address]\n"
 "           [-S ctl_path] [-W host:port] [-w local_tun[:remote_tun]]\n"
+"           [-Z obfuscate_keyword]\n"
 "           destination [command [argument ...]]\n"
 "       ssh [-Q query_option]\n"
 	);
@@ -729,7 +730,7 @@
 
  again:
 	while ((opt = getopt(ac, av, "1246ab:c:e:fgi:kl:m:no:p:qstvx"
-	    "AB:CD:E:F:GI:J:KL:MNO:P:Q:R:S:TVw:W:XYy")) != -1) { /* HUZdhjruz */
+	    "AB:CD:E:F:GI:J:KL:MNO:P:Q:R:S:TVw:W:XYyzZ:")) != -1) { /* HUZdhjruz */
 		switch (opt) {
 		case '1':
 			fatal("SSH protocol v.1 is no longer supported");
@@ -1085,6 +1086,21 @@
 		case 'F':
 			config = optarg;
 			break;
+		case 'z':
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+			options.obfuscate_handshake = 1;
+#else
+			fatal("handshake obfuscation requires openssl");
+#endif
+			break;
+		case 'Z':
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+			options.obfuscate_handshake = 1;
+			options.obfuscate_keyword = optarg;
+#else
+			fatal("handshake obfuscation requires openssl");
+#endif
+		break;
 		default:
 			usage();
 		}
Index: openssh-10.2p1/sshconnect.c
===================================================================
--- openssh-10.2p1.orig/sshconnect.c	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/sshconnect.c	2026-05-15 09:36:21.027464544 +0800
@@ -57,6 +57,9 @@
 #include "monitor_fdpass.h"
 #include "ssh2.h"
 #include "version.h"
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#include "obfuscate.h"
+#endif
 #include "authfile.h"
 #include "ssherr.h"
 #include "authfd.h"
@@ -261,6 +264,14 @@
 	if (ssh_packet_set_connection(ssh, pout[0], pin[1]) == NULL)
 		return -1; /* ssh_packet_set_connection logs error */
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if(options.obfuscate_handshake) {
+		if(options.obfuscate_keyword)
+			obfuscate_set_keyword(options.obfuscate_keyword);
+		sshpkt_enable_obfuscation(ssh);
+	}
+#endif
+
 	return 0;
 }
 
@@ -531,6 +542,14 @@
 	if (ssh_packet_set_connection(ssh, sock, sock) == NULL)
 		return -1; /* ssh_packet_set_connection logs error */
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if(options.obfuscate_handshake) {
+		if(options.obfuscate_keyword)
+			obfuscate_set_keyword(options.obfuscate_keyword);
+		sshpkt_enable_obfuscation(ssh);
+	}
+#endif
+
 	return 0;
 }
 
@@ -1611,6 +1630,11 @@
 	host = xstrdup(orighost);
 	lowercase(host);
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if(options.obfuscate_handshake)
+		obfuscate_send_seed(ssh_packet_get_connection_out(ssh));
+#endif
+
 	/* Exchange protocol version identification strings with the server. */
 	if ((r = kex_exchange_identification(ssh, timeout_ms, 1,
 	    options.version_addendum)) != 0)
Index: openssh-10.2p1/sshd-auth.c
===================================================================
--- openssh-10.2p1.orig/sshd-auth.c	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/sshd-auth.c	2026-05-15 09:36:21.027464544 +0800
@@ -91,6 +91,9 @@
 #endif
 #include "monitor_wrap.h"
 #include "auth-options.h"
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#include "obfuscate.h"
+#endif
 #include "version.h"
 #include "ssherr.h"
 #include "sk-api.h"
@@ -159,6 +162,12 @@
 /* message to be displayed after login */
 struct sshbuf *loginmsg;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+/* Enable handshake obfuscation */
+int use_obfuscation = 0;
+struct sshbuf *obfuscation_keys;
+#endif
+
 /* Prototypes for various functions defined later in this file. */
 static void do_ssh2_kex(struct ssh *);
 
@@ -645,6 +654,25 @@
 	setproctitle("%s", "[session-auth early]");
 	recv_privsep_state(ssh, cfg, &timing_secret);
 	parse_server_config(&options, "rexec", cfg, &includes, NULL, 1);
+
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	for (i = 0; i < options.num_obfuscated_ports; i++) {
+		if (options.obfuscated_ports[i] == ssh_local_port(ssh)) {
+			use_obfuscation = 1;
+			break;
+		}
+	}
+	if (use_obfuscation) {
+		if ((obfuscation_keys = sshbuf_new()) == NULL)
+			fatal("sshbuf_new obfuscation keys buf failed");
+		debug3("requesting obfuscation key state from sshd-session");
+		mm_get_obfuscation(obfuscation_keys);
+		obfuscate_recv_keys(obfuscation_keys);
+		debug3("received obfuscation key state from sshd-session");
+		sshbuf_free(obfuscation_keys);
+	}
+#endif
+
 	/* Fill in default values for those options not explicitly set. */
 	fill_default_server_options(&options);
 	options.timing_secret = timing_secret; /* XXX eliminate from unpriv */
@@ -739,6 +767,11 @@
 
 	ssh_packet_set_nonblocking(ssh);
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if (use_obfuscation)
+		sshpkt_enable_obfuscation(ssh);
+#endif
+
 	/* allocate authentication context */
 	authctxt = xcalloc(1, sizeof(*authctxt));
 	ssh->authctxt = authctxt;
@@ -776,6 +809,7 @@
 	mm_send_keystate(ssh, pmonitor);
 	sshauthopt_free(auth_opts);
 	ssh_packet_clear_keys(ssh);
+
 	exit(0);
 }
 
Index: openssh-10.2p1/sshd-session.c
===================================================================
--- openssh-10.2p1.orig/sshd-session.c	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/sshd-session.c	2026-05-15 09:36:21.031464667 +0800
@@ -97,6 +97,9 @@
 #endif
 #include "monitor_wrap.h"
 #include "auth-options.h"
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#include "obfuscate.h"
+#endif
 #include "version.h"
 #include "ssherr.h"
 #include "sk-api.h"
@@ -201,6 +204,11 @@
 /* message to be displayed after login */
 struct sshbuf *loginmsg;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+/* Enable handshake obfuscation */
+int use_obfuscation = 0;
+#endif
+
 /* Prototypes for various functions defined later in this file. */
 void destroy_sensitive_data(void);
 void demote_sensitive_data(void);
@@ -1219,6 +1227,15 @@
 	server_process_channel_timeouts(ssh);
 	server_process_permitopen(ssh);
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	for (i = 0; i < options.num_obfuscated_ports; i++) {
+		if (options.obfuscated_ports[i] == ssh_local_port(ssh)) {
+			use_obfuscation = 1;
+			break;
+		}
+	}
+#endif
+
 	/* Set SO_KEEPALIVE if requested. */
 	if (options.tcp_keep_alive && ssh_packet_connection_is_on_socket(ssh) &&
 	    setsockopt(sock_in, SOL_SOCKET, SO_KEEPALIVE, &on, sizeof(on)) == -1)
@@ -1290,6 +1307,15 @@
 			fatal("login grace time setitimer failed");
 	}
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if (use_obfuscation) {
+		if (options.obfuscate_keyword)
+			obfuscate_set_keyword(options.obfuscate_keyword);
+		sshpkt_enable_obfuscation(ssh);
+		obfuscate_receive_seed(ssh, sock_in);
+	}
+#endif
+
 	if ((r = kex_exchange_identification(ssh, -1, options.debian_banner,
 	    options.version_addendum)) != 0)
 		sshpkt_fatal(ssh, r, "banner exchange");
@@ -1315,6 +1341,11 @@
 	if (privsep_preauth(ssh) != 1)
 		fatal("privsep_preauth failed");
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if (use_obfuscation)
+		sshpkt_disable_obfuscation(ssh);
+#endif
+
 	/* Now user is authenticated */
 
 	/*
Index: openssh-10.2p1/sshd_config
===================================================================
--- openssh-10.2p1.orig/sshd_config	2026-05-15 09:36:19.000000000 +0800
+++ openssh-10.2p1/sshd_config	2026-05-15 09:36:21.031464667 +0800
@@ -34,6 +34,8 @@
 #   systemctl restart ssh.socket
 #
 #Port 22
+#ObfuscatedPort 222
+#ObfuscateKeyword key
 #AddressFamily any
 #ListenAddress 0.0.0.0
 #ListenAddress ::
