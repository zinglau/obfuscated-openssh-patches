diff -Nurp openssh-10.5p1/kex.c openssh-10.5p1-ob/kex.c
--- openssh-10.5p1/kex.c	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/kex.c	2026-08-26 10:31:04.005804603 +0800
@@ -55,6 +55,9 @@
 #include "dispatch.h"
 #include "myproposal.h"
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#include "obfuscate.h"
+#endif
 #include "ssherr.h"
 #include "sshbuf.h"
 #include "digest.h"
@@ -341,6 +344,11 @@ kex_maybe_send_ext_info(struct ssh *ssh)
 	if (!ssh->kex->ext_info_c && !ssh->kex->ext_info_s)
 		return 0;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if (sshpkt_get_obfuscation(ssh) == 1)
+		sshpkt_disable_obfuscation(ssh);
+#endif
+
 	/* Compose EXT_INFO packet. */
 	if ((m = sshbuf_new()) == NULL)
 		fatal_f("sshbuf_new failed");
@@ -364,6 +372,10 @@ kex_maybe_send_ext_info(struct ssh *ssh)
 
  fail:
 	sshbuf_free(m);
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if (sshpkt_get_obfuscation(ssh) == 0)
+		sshpkt_enable_obfuscation(ssh);
+#endif
 	return r;
 }
 
@@ -567,6 +579,10 @@ kex_input_newkeys(int type, uint32_t seq
 	kex->flags &= ~KEX_INITIAL;
 	sshbuf_reset(kex->peer);
 	kex->flags &= ~(KEX_INIT_SENT|KEX_INIT_RECVD);
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if (sshpkt_get_obfuscation(ssh) == 1)
+		sshpkt_disable_obfuscation(ssh);
+#endif
 	return 0;
 }
 
@@ -1267,14 +1283,48 @@ kex_exchange_identification(struct ssh *
 		goto out;
 	}
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	/* Make copy and obfuscate the original */
+	if (sshpkt_get_obfuscation(ssh) == 1) {
+		if ((cp = sshbuf_dup_string(our_version)) == NULL) {
+			error("%s: sshbuf_dup_string failed for obfuscation", __func__);
+			r = SSH_ERR_ALLOC_FAIL;
+			goto out;
+		}
+		obfuscate_output(sshbuf_mutable_ptr(our_version), sshbuf_len(our_version));
+	}
+#endif
+
 	if (atomicio(vwrite, ssh_packet_get_connection_out(ssh),
 	    sshbuf_mutable_ptr(our_version),
 	    sshbuf_len(our_version)) != sshbuf_len(our_version)) {
 		oerrno = errno;
 		debug_f("write: %.100s", strerror(errno));
 		r = SSH_ERR_SYSTEM_ERROR;
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+		if (sshpkt_get_obfuscation(ssh) == 1) {
+			free(cp);
+		}
+#endif
 		goto out;
 	}
+
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	/* Restore the original */
+	if (sshpkt_get_obfuscation(ssh) == 1) {
+		if ((r = sshbuf_consume(our_version, sshbuf_len(our_version))) != 0) {
+			error("%s: sshbuf_consume failed for obfuscation", __func__);
+			free(cp);
+			goto out;
+		}
+		if ((r = sshbuf_put(our_version, cp, strlen(cp))) != 0) {
+			error("%s: sshbuf_put failed for obfuscation", __func__);
+			free(cp);
+			goto out;
+		}
+	}
+#endif
+
 	if ((r = sshbuf_consume_end(our_version, 2)) != 0) { /* trim \r\n */
 		oerrno = errno;
 		error_fr(r, "sshbuf_consume_end");
@@ -1331,6 +1381,10 @@ kex_exchange_identification(struct ssh *
 				r = SSH_ERR_SYSTEM_ERROR;
 				goto out;
 			}
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+			if(sshpkt_get_obfuscation(ssh) == 1)
+				obfuscate_input(&c, 1);
+#endif
 			if (c == '\r') {
 				expect_nl = 1;
 				continue;
diff -Nurp openssh-10.5p1/Makefile.in openssh-10.5p1-ob/Makefile.in
--- openssh-10.5p1/Makefile.in	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/Makefile.in	2026-08-26 10:31:03.997804600 +0800
@@ -108,7 +108,7 @@ LIBSSH_OBJS=${LIBOPENSSH_OBJS} \
 	kexgexc.o kexgexs.o kexmlkem768ecdh.o \
 	kexsntrup761x25519.o kexmlkem768x25519.o sntrup761.o kexgen.o \
 	sftp-realpath.o platform-pledge.o platform-tracing.o platform-misc.o \
-	sshbuf-io.o misc-agent.o ssherr-libcrypto.o
+	sshbuf-io.o misc-agent.o ssherr-libcrypto.o obfuscate.o
 
 P11OBJS= ssh-pkcs11-client.o
 
diff -Nurp openssh-10.5p1/obfuscate.c openssh-10.5p1-ob/obfuscate.c
--- openssh-10.5p1/obfuscate.c	1970-01-01 08:00:00.000000000 +0800
+++ openssh-10.5p1-ob/obfuscate.c	2026-08-26 10:31:04.005804603 +0800
@@ -0,0 +1,232 @@
+#include "includes.h"
+#include <openssl/evp.h>
+#include <openssl/rc4.h>
+#include "openbsd-compat/openssl-compat.h"
+#include <unistd.h>
+#include <string.h>
+#include "atomicio.h"
+#include "canohost.h"
+#include "xmalloc.h"
+#include "log.h"
+#include "sshbuf.h"
+#include "packet.h"
+#include "obfuscate.h"
+
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+typedef struct {
+    RC4_KEY input;
+    RC4_KEY output;
+} RC4_KEYS;
+
+static const char *obfuscate_keyword = NULL;
+
+#define OBFUSCATE_KEY_LENGTH 	16
+#define OBFUSCATE_SEED_LENGTH	16
+#define OBFUSCATE_HASH_ITERATIONS 6000
+#define OBFUSCATE_MAX_PADDING	8192
+#define OBFUSCATE_MAGIC_VALUE	0x0BF5CA7E
+
+struct seed_msg {
+	u_char seed_buffer[OBFUSCATE_SEED_LENGTH];
+	u_int32_t magic;
+	u_int32_t padding_length;
+	u_char padding[];
+};
+
+static RC4_KEYS *rc4_keys;
+
+static void generate_key_pair(const u_char *, u_char *, u_char *);
+static void generate_key(const u_char *, const u_char *, u_int, u_char *);
+static void set_keys(const u_char *, const u_char *);
+static void initialize(const u_char *, int);
+static void read_forever(int);
+
+/*
+ * Server calls this
+ */
+void
+obfuscate_receive_seed(struct ssh *ssh, int sock_in)
+{
+	struct seed_msg seed;
+
+	u_char padding_drain[OBFUSCATE_MAX_PADDING];
+	u_int len;
+	u_int32_t padding_length;
+
+	len = atomicio(read, sock_in, &seed, sizeof(struct seed_msg));
+
+	debug2("obfuscate_receive_seed: read %d byte seed message from client", len);
+	if(len != sizeof(struct seed_msg))
+		fatal("obfuscate_receive_seed: read failed");
+
+	initialize(seed.seed_buffer, 1);
+	obfuscate_input((u_char *)&seed.magic, 8);
+
+	if(OBFUSCATE_MAGIC_VALUE != ntohl(seed.magic)) {
+		logit("Magic value check failed (%u) on obfuscated handshake "
+ 			"from %.200s port %d", ntohl(seed.magic),
+			ssh_remote_ipaddr(ssh), ssh_remote_port(ssh));
+		read_forever(sock_in);
+	}
+	padding_length = ntohl(seed.padding_length);
+	if(padding_length > OBFUSCATE_MAX_PADDING) {
+		logit("Illegal padding length %d for obfuscated handshake "
+ 			"from %.200s port %d", ntohl(seed.padding_length),
+			ssh_remote_ipaddr(ssh), ssh_remote_port(ssh));
+		read_forever(sock_in);
+	}
+	len = atomicio(read, sock_in, padding_drain, padding_length);
+	if(len != padding_length)
+		fatal("obfuscate_receive_seed: read failed");
+	debug2("obfuscate_receive_seed: read %d bytes of padding from client.", len);
+	obfuscate_input(padding_drain, padding_length);
+}
+
+/*
+ * Client calls this
+ */
+void
+obfuscate_send_seed(int sock_out)
+{
+	struct seed_msg *seed;
+	int i;
+	u_int32_t rnd = 0;
+	u_int message_length;
+	u_int padding_length;
+
+	padding_length = arc4random() % OBFUSCATE_MAX_PADDING;
+	message_length = padding_length + sizeof(struct seed_msg);
+	seed = xmalloc(message_length);
+
+	for(i = 0; i < OBFUSCATE_SEED_LENGTH; i++) {
+		if(i % 4 == 0)
+			rnd = arc4random();
+		seed->seed_buffer[i] = rnd & 0xff;
+		rnd >>= 8;
+	}
+	seed->magic = htonl(OBFUSCATE_MAGIC_VALUE);
+	seed->padding_length = htonl(padding_length);
+	for(i = 0; i < (int)padding_length; i++) {
+		if(i % 4 == 0)
+			rnd = arc4random();
+		seed->padding[i] = rnd & 0xff;
+	}
+	initialize(seed->seed_buffer, 0);
+	obfuscate_output(((u_char *)seed) + OBFUSCATE_SEED_LENGTH,
+		message_length - OBFUSCATE_SEED_LENGTH);
+	debug2("obfuscate_send_seed: Sending seed message with %d bytes of padding", padding_length);
+	atomicio(vwrite, sock_out, seed, message_length);
+	free(seed);
+
+}
+
+void
+obfuscate_set_keyword(const char *keyword)
+{
+	debug2("obfuscate_set_keyword: Setting obfuscation keyword to '%s'", keyword);
+	obfuscate_keyword = keyword;
+}
+
+void
+obfuscate_input(u_char *buffer, u_int buffer_len)
+{
+	if (rc4_keys)
+		RC4(&rc4_keys->input, buffer_len, buffer, buffer);
+	else fatal("no obfuscation key");
+}
+
+void
+obfuscate_output(u_char *buffer, u_int buffer_len)
+{
+	if (rc4_keys)
+		RC4(&rc4_keys->output, buffer_len, buffer, buffer);
+	else fatal_f("no obfuscation key");
+}
+
+static void
+initialize(const u_char *seed, int server)
+{
+    u_char client_to_server_key[OBFUSCATE_KEY_LENGTH];
+	u_char server_to_client_key[OBFUSCATE_KEY_LENGTH];
+	generate_key_pair(seed, client_to_server_key, server_to_client_key);
+
+	rc4_keys = xmalloc(sizeof(RC4_KEYS));
+	if(server) {
+		set_keys(client_to_server_key, server_to_client_key);
+	} else {
+		set_keys(server_to_client_key, client_to_server_key);
+	}
+}
+
+static void
+generate_key_pair(const u_char *seed, u_char *client_to_server_key, u_char *server_to_client_key)
+{
+	generate_key(seed, "client_to_server", strlen("client_to_server"), client_to_server_key);
+	generate_key(seed, "server_to_client", strlen("server_to_client"), server_to_client_key);
+}
+
+static void
+generate_key(const u_char *seed, const u_char *iv, u_int iv_len, u_char *key_data)
+{
+	EVP_MD_CTX *ctx;
+	u_char md_output[EVP_MAX_MD_SIZE];
+	int md_len;
+	int i;
+	u_char *buffer;
+	u_char *p;
+	u_int buffer_length;
+
+	if ((ctx = EVP_MD_CTX_new()) == NULL)
+		fatal("Cannot create new digest context");
+
+	buffer_length = OBFUSCATE_SEED_LENGTH + iv_len;
+	if(obfuscate_keyword)
+		buffer_length += strlen(obfuscate_keyword);
+
+	p = buffer = xmalloc(buffer_length);
+
+	memcpy(p, seed, OBFUSCATE_SEED_LENGTH);
+	p += OBFUSCATE_SEED_LENGTH;
+
+	if(obfuscate_keyword) {
+		memcpy(p, obfuscate_keyword, strlen(obfuscate_keyword));
+		p += strlen(obfuscate_keyword);
+	}
+	memcpy(p, iv, iv_len);
+
+	EVP_DigestInit(ctx, EVP_sha1());
+	EVP_DigestUpdate(ctx, buffer, OBFUSCATE_SEED_LENGTH + iv_len);
+	EVP_DigestFinal(ctx, md_output, &md_len);
+
+	free(buffer);
+
+	for(i = 0; i < OBFUSCATE_HASH_ITERATIONS; i++) {
+		EVP_DigestInit(ctx, EVP_sha1());
+		EVP_DigestUpdate(ctx, md_output, md_len);
+		EVP_DigestFinal(ctx, md_output, &md_len);
+	}
+
+	if(md_len < OBFUSCATE_KEY_LENGTH)
+		fatal("Cannot derive obfuscation keys from hash length of %d", md_len);
+
+	memcpy(key_data, md_output, OBFUSCATE_KEY_LENGTH);
+}
+
+static void
+set_keys(const u_char *input_key, const u_char *output_key)
+{
+	RC4_set_key(&rc4_keys->input, OBFUSCATE_KEY_LENGTH, input_key);
+	RC4_set_key(&rc4_keys->output, OBFUSCATE_KEY_LENGTH, output_key);
+}
+
+static void
+read_forever(int sock_in)
+{
+	u_char discard_buffer[1024];
+
+	while(atomicio(read, sock_in, discard_buffer, sizeof(discard_buffer)) > 0)
+		;
+	cleanup_exit(255);
+}
+
+#endif
diff -Nurp openssh-10.5p1/obfuscate.h openssh-10.5p1-ob/obfuscate.h
--- openssh-10.5p1/obfuscate.h	1970-01-01 08:00:00.000000000 +0800
+++ openssh-10.5p1-ob/obfuscate.h	2026-08-26 10:31:04.005804603 +0800
@@ -0,0 +1,12 @@
+#ifndef _OBFUSCATE_H
+#define _OBFUSCATE_H
+
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+void obfuscate_receive_seed(struct ssh *, int);
+void obfuscate_send_seed(int);
+void obfuscate_set_keyword(const char *);
+void obfuscate_input(u_char *, u_int);
+void obfuscate_output(u_char *, u_int);
+#endif
+
+#endif
diff -Nurp openssh-10.5p1/packet.c openssh-10.5p1-ob/packet.c
--- openssh-10.5p1/packet.c	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/packet.c	2026-08-26 10:31:04.005804603 +0800
@@ -85,6 +85,9 @@
 #include "canohost.h"
 #include "misc.h"
 #include "packet.h"
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#include "obfuscate.h"
+#endif
 #include "ssherr.h"
 #include "sshbuf.h"
 
@@ -168,6 +171,12 @@ struct session_state {
 	/* Set to true if we are the server side. */
 	int server_side;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	/* Set to 1 (0) if subsequent packets must (not) be obfuscated. */
+	/* Will be initialized to -1 and should only be toggled between 0/1 */
+	int obfuscation;
+#endif
+
 	/* Set to true if we are authenticated. */
 	int after_authentication;
 
@@ -255,6 +264,9 @@ ssh_alloc_session_state(void)
 	state->qos_interactive = state->qos_other = -1;
 	state->p_send.packets = state->p_read.packets = 0;
 	state->initialized = 1;
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	state->obfuscation = -1;
+#endif
 	/*
 	 * ssh_packet_send2() needs to queue packets until
 	 * we've done the initial key exchange.
@@ -1353,6 +1365,12 @@ ssh_packet_send2_wrapped(struct ssh *ssh
 		if ((r = sshbuf_put(state->output, macbuf, mac->mac_len)) != 0)
 			goto out;
 	}
+
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if(state->obfuscation == 1)
+		obfuscate_output(cp, sshbuf_len(state->outgoing_packet));
+#endif
+
 #ifdef PACKET_DEBUG
 	fprintf(stderr, "encrypted: ");
 	sshbuf_dump(state->output, stderr);
@@ -1677,6 +1695,10 @@ ssh_packet_read_poll2(struct ssh *ssh, u
 		if ((r = sshbuf_reserve(state->incoming_packet, block_size,
 		    &cp)) != 0)
 			goto out;
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+		if(state->obfuscation == 1)
+			obfuscate_input(sshbuf_mutable_ptr(state->input), block_size);
+#endif
 		if ((r = cipher_crypt(state->receive_context,
 		    state->p_send.seqnr, cp, sshbuf_ptr(state->input),
 		    block_size, 0, 0)) != 0)
@@ -1742,6 +1764,10 @@ ssh_packet_read_poll2(struct ssh *ssh, u
 			goto out;
 		}
 	}
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if(state->obfuscation == 1)
+		obfuscate_input(sshbuf_mutable_ptr(state->input), need);
+#endif
 	if ((r = sshbuf_reserve(state->incoming_packet, aadlen + need,
 	    &cp)) != 0)
 		goto out;
@@ -2963,6 +2989,28 @@ sshpkt_add_padding(struct ssh *ssh, u_ch
 	return 0;
 }
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+void
+sshpkt_enable_obfuscation(struct ssh *ssh)
+{
+	ssh->state->obfuscation = 1;
+	debug("Obfuscation enabled");
+}
+
+void
+sshpkt_disable_obfuscation(struct ssh *ssh)
+{
+	ssh->state->obfuscation = 0;
+	debug("Obfuscation disabled");
+}
+
+int
+sshpkt_get_obfuscation(struct ssh *ssh)
+{
+	return ssh->state->obfuscation;
+}
+#endif
+
 static char *
 format_traffic_stats(struct packet_state *ps)
 {
diff -Nurp openssh-10.5p1/packet.h openssh-10.5p1-ob/packet.h
--- openssh-10.5p1/packet.h	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/packet.h	2026-08-26 10:31:04.005804603 +0800
@@ -178,6 +178,11 @@ void	sshpkt_fatal(struct ssh *ssh, int r
 	    __attribute__((format(printf, 3, 4)))
 	    __attribute__((noreturn));
 int	sshpkt_msg_ignore(struct ssh *, u_int);
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+void	sshpkt_enable_obfuscation(struct ssh *);
+void	sshpkt_disable_obfuscation(struct ssh *);
+int sshpkt_get_obfuscation(struct ssh *);
+#endif
 
 int	sshpkt_put(struct ssh *ssh, const void *v, size_t len);
 int	sshpkt_putb(struct ssh *ssh, const struct sshbuf *b);
diff -Nurp openssh-10.5p1/readconf.c openssh-10.5p1-ob/readconf.c
--- openssh-10.5p1/readconf.c	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/readconf.c	2026-08-26 10:32:22.961848936 +0800
@@ -134,6 +134,9 @@ typedef enum {
 	oHost, oMatch, oInclude, oTag,
 	oForwardAgent, oForwardX11, oForwardX11Trusted, oForwardX11Timeout,
 	oGatewayPorts, oExitOnForwardFailure,
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	oObfuscateHandshake, oObfuscateKeyword,
+#endif
 	oPasswordAuthentication,
 	oXAuthLocation,
 	oIdentityFile, oHostname, oPort, oRemoteForward, oLocalForward,
@@ -320,6 +323,10 @@ static struct {
 	{ "versionaddendum", oVersionAddendum },
 	{ "refuseconnection", oRefuseConnection },
 	{ "warnweakcrypto", oWarnWeakCrypto },
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	{ "obfuscatehandshake", oObfuscateHandshake },
+	{ "obfuscatekeyword", oObfuscateKeyword },
+#endif
 
 	{ NULL, oBadOption }
 };
@@ -2564,6 +2571,18 @@ parse_pubkey_algos:
 		}
 		break;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	case oObfuscateHandshake:
+		intptr = &options->obfuscate_handshake;
+		goto parse_flag;
+
+	case oObfuscateKeyword:
+		if (*activep)
+			options->obfuscate_handshake = 1;
+		charptr = &options->obfuscate_keyword;
+		goto parse_string;
+#endif
+
 	case oDeprecated:
 		debug("%s line %d: Deprecated option \"%s\"",
 		    filename, linenum, keyword);
@@ -2795,6 +2814,10 @@ initialize_options(Options * options)
 	options->add_keys_to_agent_lifespan = -1;
 	options->identity_agent = NULL;
 	options->visual_host_key = -1;
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	options->obfuscate_handshake = 0;
+	options->obfuscate_keyword = NULL;
+#endif
 	options->ip_qos_interactive = -1;
 	options->ip_qos_bulk = -1;
 	options->request_tty = -1;
diff -Nurp openssh-10.5p1/readconf.h openssh-10.5p1-ob/readconf.h
--- openssh-10.5p1/readconf.h	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/readconf.h	2026-08-26 10:31:04.005804603 +0800
@@ -147,6 +147,11 @@ typedef struct {
 	char	*remote_command;
 	int	visual_host_key;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	int obfuscate_handshake;
+	char *obfuscate_keyword;
+#endif
+
 	int	request_tty;
 	int	session_type;
 	int	stdin_null;
diff -Nurp openssh-10.5p1/scp.c openssh-10.5p1-ob/scp.c
--- openssh-10.5p1/scp.c	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/scp.c	2026-08-26 10:31:04.005804603 +0800
@@ -488,7 +488,7 @@ main(int argc, char **argv)
 
 	fflag = Tflag = tflag = 0;
 	while ((ch = getopt(argc, argv,
-	    "12346ABCTdfOpqRrstvD:F:J:M:P:S:c:i:l:o:X:")) != -1) {
+	    "12346ABCTdfOpqRrstvD:F:J:M:P:S:c:i:l:o:X:zZ:")) != -1) {
 		switch (ch) {
 		/* User-visible flags. */
 		case '1':
@@ -501,6 +501,7 @@ main(int argc, char **argv)
 		case '4':
 		case '6':
 		case 'C':
+		case 'z':
 			addargs(&args, "-%c", ch);
 			addargs(&remote_remote_args, "-%c", ch);
 			break;
@@ -518,6 +519,7 @@ main(int argc, char **argv)
 		case 'i':
 		case 'F':
 		case 'J':
+		case 'Z':
 			addargs(&remote_remote_args, "-%c", ch);
 			addargs(&remote_remote_args, "%s", optarg);
 			addargs(&args, "-%c", ch);
diff -Nurp openssh-10.5p1/servconf.c openssh-10.5p1-ob/servconf.c
--- openssh-10.5p1/servconf.c	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/servconf.c	2026-08-26 10:33:40.626793243 +0800
@@ -161,6 +161,11 @@ initialize_server_options(ServerOptions
 	options->subsystem_args = NULL;
 #define init_timingsecret(options) \
 	options->timing_secret = 0;
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#define init_obfuscated_port(options) \
+	options->obfuscate_keyword = NULL; \
+	options->num_obfuscated_ports = 0;
+#endif
 
 	SSHD_CONFIG_ENTRIES
 
@@ -180,6 +185,9 @@ initialize_server_options(ServerOptions
 #undef init_rekeylimit
 #undef init_subsystem
 #undef init_timingsecret
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#undef init_obfuscated_port
+#endif
 #undef SSHCONF_INT
 #undef SSHCONF_INTFLAG
 #undef SSHCONF_STRING
@@ -321,7 +329,11 @@ fill_default_server_options(ServerOption
 		    _PATH_HOST_MLDSA44_ED25519_KEY_FILE, 0);
 	}
 	/* No certificates by default */
-	if (options->num_ports == 0)
+	if (options->num_ports == 0
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+		&& options->num_obfuscated_ports == 0
+#endif
+	)
 		options->ports[options->num_ports++] = SSH_DEFAULT_PORT;
 	if (options->listen_addrs == NULL)
 		add_listen_addr(options, NULL, NULL, 0);
@@ -438,6 +450,9 @@ fill_default_server_options(ServerOption
 	CLEAR_ON_NONE(options->routing_domain);
 	CLEAR_ON_NONE(options->host_key_agent);
 	CLEAR_ON_NONE(options->per_source_penalty_exempt);
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	CLEAR_ON_NONE(options->obfuscate_keyword);
+#endif
 
 	for (i = 0; i < options->num_host_key_files; i++)
 		CLEAR_ON_NONE(options->host_key_files[i]);
@@ -607,6 +622,12 @@ add_listen_addr(ServerOptions *options,
 			add_one_listen_addr(options, addr, rdomain,
 			    options->ports[i]);
 		}
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+		for (i = 0; i < options->num_obfuscated_ports; i++) {
+			add_one_listen_addr(options, addr, rdomain,
+				options->obfuscated_ports[i]);
+		}
+#endif
 	}
 }
 
@@ -721,7 +742,11 @@ process_queued_listen_addrs(ServerOption
 	u_int i;
 	struct queued_listenaddr *qla;
 
-	if (options->num_ports == 0)
+	if (options->num_ports == 0
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+		&& options->num_obfuscated_ports == 0
+#endif
+	)
 		options->ports[options->num_ports++] = SSH_DEFAULT_PORT;
 	if (options->address_family == -1)
 		options->address_family = AF_UNSPEC;
@@ -1206,6 +1231,32 @@ process_server_config_line_depth(ServerO
 			    filename, linenum);
 		break;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	case sObfuscatedPort:
+		if(options->ports_from_cmdline)
+			return 0;
+		if(options->listen_addrs != NULL)
+			fatal("%s line %d: ports must be specified before ListenAddress.", filename, linenum);
+		if(options->num_obfuscated_ports >= MAX_PORTS)
+			fatal("%s line %d: too many ports.", filename, linenum);
+		arg = argv_next(&ac, &av);
+		if(!arg || *arg == '\0')
+			fatal("%s line %d: missing port number.", filename, linenum);
+		options->obfuscated_ports[options->num_obfuscated_ports++] = a2port(arg);
+		if(options->obfuscated_ports[options->num_obfuscated_ports - 1] <= 0)
+			fatal("%s line %d: badly formatted port number.", filename, linenum);
+		break;
+	case sObfuscateKeyword:
+		charptr = &options->obfuscate_keyword;
+		arg = argv_next(&ac, &av);
+		if(!arg || *arg == '\0')
+			fatal("%s line %d: missing keyword argument.",
+					filename, linenum);
+		if(*activep && *charptr == NULL)
+			*charptr = xstrdup(arg);
+		break;
+#endif
+
 	case sLoginGraceTime:
 		intptr = &options->login_grace_time;
  parse_time:
@@ -2904,6 +2955,27 @@ serialise_port(const ServerOptions *opti
 	return 0;
 }
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+static int
+serialise_obfuscated_port(const ServerOptions *options, struct sshbuf *buf)
+{
+        int r;
+        u_int i;
+
+        if ((r = sshbuf_put_u32(buf, options->num_obfuscated_ports)) != 0) {
+                error_fr(r, "serialise length");
+                return r;
+        }
+        for (i = 0; i < options->num_obfuscated_ports; i++) {
+                if ((r = serialise_s32(buf, options->obfuscated_ports[i])) != 0) {
+                        error_fr(r, "serialise obfuscated port");
+                        return r;
+                }
+        }
+        return 0;
+}
+#endif
+
 static int
 serialise_gatewayports(const ServerOptions *options, struct sshbuf *buf)
 {
@@ -3423,6 +3495,31 @@ deserialise_port(ServerOptions *options,
 	return 0;
 }
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+static int
+deserialise_obfuscated_port(ServerOptions *options, struct sshbuf *buf)
+{
+        int r;
+        u_int i, n;
+
+        if ((r = deserialise_count(buf, &n, "obfuscated_ports")) != 0)
+                return r;
+        if (n > MAX_PORTS) {
+                error_f("bad number of obfuscated ports");
+                return SSH_ERR_INVALID_FORMAT;
+        }
+        options->num_obfuscated_ports = n;
+        memset(options->obfuscated_ports, 0, sizeof(options->obfuscated_ports));
+        for (i = 0; i < options->num_obfuscated_ports; i++) {
+                if ((r = deserialise_s32(buf, options->obfuscated_ports + i)) != 0) {
+                        error_fr(r, "deserialise obfuscated port");
+                        return r;
+                }
+        }
+        return 0;
+}
+#endif
+
 static int
 deserialise_gatewayports(ServerOptions *options, struct sshbuf *buf)
 {
@@ -3759,6 +3856,9 @@ free_server_options(ServerOptions *optio
 #define free_persourcepenalties(options)
 #define free_rekeylimit(options)
 #define free_timingsecret(options)
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#define free_obfuscated_port(options)
+#endif
 
 	SSHD_CONFIG_ENTRIES
 
@@ -3774,6 +3874,9 @@ free_server_options(ServerOptions *optio
 #undef free_persourcepenalties
 #undef free_rekeylimit
 #undef free_timingsecret
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#undef free_obfuscated_port
+#endif
 
 #undef SSHCONF_INT
 #undef SSHCONF_INTFLAG
@@ -4173,6 +4276,10 @@ dump_config(ServerOptions *o)
 	/* these are usually at the top of the config */
 	for (i = 0; i < o->num_ports; i++)
 		printf("Port %d\n", o->ports[i]);
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	for (i = 0; i < o->num_obfuscated_ports; i++)
+		printf("obfuscated port %d\n", o->obfuscated_ports[i]);
+#endif
 	dump_cfg_fmtint(sAddressFamily, o->address_family);
 
 	for (i = 0; i < o->num_listen_addrs; i++) {
@@ -4275,6 +4382,9 @@ dump_config(ServerOptions *o)
 	dump_cfg_string(sSshdSessionPath, o->sshd_session_path);
 	dump_cfg_string(sSshdAuthPath, o->sshd_auth_path);
 	dump_cfg_string(sPerSourcePenaltyExemptList, o->per_source_penalty_exempt);
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	dump_cfg_string(sObfuscateKeyword, o->obfuscate_keyword);
+#endif
 
 	/* string arguments requiring a lookup */
 	dump_cfg_string(sLogLevel, log_level_name(o->log_level));
diff -Nurp openssh-10.5p1/servconf.h openssh-10.5p1-ob/servconf.h
--- openssh-10.5p1/servconf.h	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/servconf.h	2026-08-26 10:31:04.009804606 +0800
@@ -267,9 +267,20 @@ SSHCONF_ALIAS(KeepAlive, TCPKeepAlive, S
 	SSHD_CONFIG_ENTRIES_MAIN \
 	SSHD_CONFIG_ENTRIES_LEGACY \
 	SSHD_CONFIG_ENTRIES_ALIASES \
+	SSHD_CONFIG_ENTRIES_OBFUSCATION \
 	SSHD_CONFIG_ENTRIES_PAM \
 	SSHD_CONFIG_ENTRIES_LASTLOG
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#define SSHD_CONFIG_ENTRIES_OBFUSCATION \
+SSHCONF_CUSTOM(ObfuscatedPort, obfuscated_port, SSHCFG_GLOBAL, SSHCFG_COPY_NONE) \
+SSHCONF_STRING(obfuscate_keyword, ObfuscateKeyword, SSHCFG_GLOBAL, SSHCFG_COPY_NONE)
+#else
+#define SSHD_CONFIG_ENTRIES_OBFUSCATION \
+SSHCONF_DEPRECATE(ObfuscatedPort, SSHCFG_GLOBAL, SSHCONF_IGNORE) \
+SSHCONF_DEPRECATE(ObfuscateKeyword, SSHCFG_GLOBAL, SSHCONF_IGNORE)
+#endif
+
 #ifdef USE_PAM
 #define SSHD_CONFIG_ENTRIES_PAM \
 SSHCONF_INTFLAG(use_pam, UsePAM, SSHCFG_GLOBAL, 0, SSHCFG_COPY_NONE) \
@@ -351,6 +362,11 @@ typedef struct ServerOptions {
 	u_int	num_ports;
 	u_int	ports_from_cmdline;
 	int	ports[MAX_PORTS];	/* Port number to listen on. */
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	u_int	num_obfuscated_ports;
+	int	obfuscated_ports[MAX_PORTS];
+	//char	*obfuscate_keyword;
+#endif
 	/* ListenAddress */
 	struct queued_listenaddr *queued_listen_addrs;
 	u_int	num_queued_listens;
diff -Nurp openssh-10.5p1/sftp.c openssh-10.5p1-ob/sftp.c
--- openssh-10.5p1/sftp.c	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/sftp.c	2026-08-26 10:31:04.009804606 +0800
@@ -2494,13 +2494,14 @@ main(int argc, char **argv)
 	infile = stdin;
 
 	while ((ch = getopt(argc, argv,
-	    "1246AafhNpqrvCc:D:i:l:o:s:S:b:B:F:J:P:R:X:")) != -1) {
+	    "1246AafhNpqrvCc:D:i:l:o:s:S:b:B:F:J:P:R:X:zZ:")) != -1) {
 		switch (ch) {
 		/* Passed through to ssh(1) */
 		case 'A':
 		case '4':
 		case '6':
 		case 'C':
+		case 'z':
 			addargs(&args, "-%c", ch);
 			break;
 		/* Passed through to ssh(1) with argument */
@@ -2509,6 +2510,7 @@ main(int argc, char **argv)
 		case 'c':
 		case 'i':
 		case 'o':
+		case 'Z':
 			addargs(&args, "-%c", ch);
 			addargs(&args, "%s", optarg);
 			break;
diff -Nurp openssh-10.5p1/ssh.c openssh-10.5p1-ob/ssh.c
--- openssh-10.5p1/ssh.c	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/ssh.c	2026-08-26 11:37:28.034905619 +0800
@@ -168,12 +168,13 @@ static void
 usage(void)
 {
 	fprintf(stderr,
-"usage: ssh [-46AaCfGgKkMNnqsTtVvXxYyZ] [-B bind_interface] [-b bind_address]\n"
+"usage: ssh [-46AaCfGgKkMNnqsTtVvXxYyZz] [-B bind_interface] [-b bind_address]\n"
 "           [-c cipher_spec] [-D [bind_address:]port] [-E log_file]\n"
 "           [-e escape_char] [-F configfile] [-I pkcs11] [-i identity_file]\n"
 "           [-J destination] [-L address] [-l login_name] [-m mac_spec]\n"
 "           [-O ctl_cmd] [-o option] [-P tag] [-p port] [-R address]\n"
 "           [-S ctl_path] [-W host:port] [-w local_tun[:remote_tun]]\n"
+"           [-Z obfuscate_keyword]\n"
 "           destination [command [argument ...]]\n"
 "       ssh [-Q query_option]\n"
 	);
@@ -700,8 +701,8 @@ main(int ac, char **av)
 	argv0 = av[0];
 
  again:
-	/* remaining: HUdhjruz */
-	while ((opt = getopt(ac, av, "1246ACGKMNTVXYZafgknqstvxy"
+	/* remaining: HUdhjru */
+	while ((opt = getopt(ac, av, "1246ACGKMNTVXYZafgknqstvxyz"
 	    "B:D:E:F:I:J:L:O:P:Q:R:S:W:b:c:e:i:l:m:o:p:w:")) != -1) {
 		switch (opt) {
 		case '1':
@@ -743,7 +744,17 @@ main(int ac, char **av)
 			options.forward_x11_trusted = 1;
 			break;
 		case 'Z':
+			if (optind < ac && av[optind][0] != '-') {
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+				options.obfuscate_handshake = 1;
+				options.obfuscate_keyword = av[optind];
+				optind++;
+#else
+				fatal("handshake obfuscation requires openssl");
+#endif
+			} else {
 			dump_pubkeys = 1;
+			}
 			break;
 		case 'g':
 			options.fwd_opts.gateway_ports = 1;
@@ -1063,6 +1074,13 @@ main(int ac, char **av)
 		case 'F':
 			config = optarg;
 			break;
+		case 'z':
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+			options.obfuscate_handshake = 1;
+#else
+			fatal("handshake obfuscation requires openssl");
+#endif
+			break;
 		default:
 			usage();
 		}
diff -Nurp openssh-10.5p1/sshconnect.c openssh-10.5p1-ob/sshconnect.c
--- openssh-10.5p1/sshconnect.c	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/sshconnect.c	2026-08-26 10:31:04.013804608 +0800
@@ -55,6 +55,9 @@
 #include "ssherr.h"
 #include "authfd.h"
 #include "kex.h"
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#include "obfuscate.h"
+#endif
 
 struct sshkey *previous_host_key = NULL;
 
@@ -298,6 +301,14 @@ ssh_proxy_connect(struct ssh *ssh, const
 	if (ssh_packet_set_connection(ssh, pout[0], pin[1]) == NULL)
 		return -1; /* ssh_packet_set_connection logs error */
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if(options.obfuscate_handshake) {
+		if(options.obfuscate_keyword)
+			obfuscate_set_keyword(options.obfuscate_keyword);
+		sshpkt_enable_obfuscation(ssh);
+	}
+#endif
+
 	return 0;
 }
 
@@ -568,6 +579,14 @@ ssh_connect_direct(struct ssh *ssh, cons
 	if (ssh_packet_set_connection(ssh, sock, sock) == NULL)
 		return -1; /* ssh_packet_set_connection logs error */
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if(options.obfuscate_handshake) {
+		if(options.obfuscate_keyword)
+			obfuscate_set_keyword(options.obfuscate_keyword);
+		sshpkt_enable_obfuscation(ssh);
+	}
+#endif
+
 	return 0;
 }
 
@@ -1642,6 +1661,11 @@ ssh_login(struct ssh *ssh, Sensitive *se
 	host = xstrdup(orighost);
 	lowercase(host);
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if(options.obfuscate_handshake)
+		obfuscate_send_seed(ssh_packet_get_connection_out(ssh));
+#endif
+
 	/* Exchange protocol version identification strings with the server. */
 	if ((r = kex_exchange_identification(ssh, timeout_ms,
 	    options.version_addendum)) != 0)
diff -Nurp openssh-10.5p1/sshd-auth.c openssh-10.5p1-ob/sshd-auth.c
--- openssh-10.5p1/sshd-auth.c	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/sshd-auth.c	2026-08-26 10:31:04.013804608 +0800
@@ -90,6 +90,9 @@
 #endif
 #include "monitor_wrap.h"
 #include "auth-options.h"
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+#include "obfuscate.h"
+#endif
 #include "version.h"
 #include "ssherr.h"
 #include "sk-api.h"
@@ -158,6 +161,11 @@ struct include_list includes = TAILQ_HEA
 /* message to be displayed after login */
 struct sshbuf *loginmsg;
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+/* Enable handshake obfuscation */
+int use_obfuscation = 0;
+#endif
+
 /* Prototypes for various functions defined later in this file. */
 static void do_ssh2_kex(struct ssh *);
 
@@ -616,6 +624,22 @@ main(int ac, char **av)
 	setproctitle("%s", "[session-auth early]");
 	recv_privsep_state(ssh, &options, &timing_secret);
 	options.timing_secret = timing_secret; /* XXX eliminate from unpriv */
+
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+        for (i = 0; i < options.num_obfuscated_ports; i++) {
+                if (options.obfuscated_ports[i] == ssh_local_port(ssh)) {
+                        use_obfuscation = 1;
+                        break;
+                }
+        }
+        if (use_obfuscation) {
+                if (options.obfuscate_keyword)
+                        obfuscate_set_keyword(options.obfuscate_keyword);
+                sshpkt_enable_obfuscation(ssh);
+                obfuscate_receive_seed(ssh, sock_in);
+        }
+#endif
+
 	ssh_packet_set_qos(ssh, options.ip_qos_interactive,
 	    options.ip_qos_bulk);
 
@@ -707,6 +731,11 @@ main(int ac, char **av)
 
 	ssh_packet_set_nonblocking(ssh);
 
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if (use_obfuscation)
+		sshpkt_enable_obfuscation(ssh);
+#endif
+
 	/* allocate authentication context */
 	authctxt = xcalloc(1, sizeof(*authctxt));
 	ssh->authctxt = authctxt;
@@ -741,6 +770,10 @@ main(int ac, char **av)
 	mm_send_keystate(ssh, pmonitor);
 	sshauthopt_free(auth_opts);
 	ssh_packet_clear_keys(ssh);
+#if defined(WITH_OPENSSL) && !defined(OPENSSL_NO_DEPRECATED_3_0)
+	if (use_obfuscation)
+		sshpkt_enable_obfuscation(ssh);
+#endif
 	exit(0);
 }
 
diff -Nurp openssh-10.5p1/sshd_config openssh-10.5p1-ob/sshd_config
--- openssh-10.5p1/sshd_config	2026-08-11 10:22:17.000000000 +0800
+++ openssh-10.5p1-ob/sshd_config	2026-08-26 10:31:04.013804608 +0800
@@ -11,6 +11,8 @@
 # default value.
 
 #Port 22
+#ObfuscatedPort 222
+#ObfuscateKeyword key
 #AddressFamily any
 #ListenAddress 0.0.0.0
 #ListenAddress ::
